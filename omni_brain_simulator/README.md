# omni_brain_simulator package README

This package contains the Omni Brain Simulator core modules (engine, visualizer) used by the demo runner.

Run the demo (desktop):

pip install -r omni_brain_simulator/requirements.txt
python main.py
